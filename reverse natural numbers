#include <stdio.h>
int main()
{
   int n, reverse=0, a;

   printf("Enter a number\n");
   scanf("%d", &n);

   for (n; n!= 0; n = n/10) {
      a = n % 10;
       reverse=reverse*10+a;
   }

   printf("REVERSE of digits of a number = %d\n", reverse);

   return 0;
}
